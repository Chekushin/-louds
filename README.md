# 🐳 Лабораторные работы по Docker и Flask

**Выполнили:** Чекушин В., Малыхин Н.

---

## 📖 Введение

В этих лабораторных мы изучали **контейнеризацию приложений с помощью Docker** и работу с **Python Flask**.

Цель — понять, как создавать контейнеры, запускать их, настраивать сети между ними и обеспечивать **изоляцию и стабильность** приложений.

---

## 🛠 Лабораторная работа 1: «Плохой и хороший Dockerfile»

1. **Анализ плохого Dockerfile**

   * Использовался образ с тегом `latest`, что снижает воспроизводимость.
   * Лишние слои и неэффективное копирование файлов.
   * Возможны проблемы с правами доступа и зависимостями.

2. **Создание хорошего Dockerfile**

   * Фиксированная версия базового образа.
   * Оптимизация слоёв и правильное копирование файлов.
   * Установка зависимостей с минимизацией размера образа.

✅ **Результат:** контейнер запускается стабильно, Flask-приложение отвечает на запросы, структура Dockerfile понятна и безопасна.

---

## 🔗 Лабораторная работа 2: «Docker Compose и изоляция»

1. **Запуск нескольких контейнеров через Docker Compose**

   * Контейнеры поднялись одновременно, но **не видят друг друга** по сети.
   * Настроены отдельные сети для каждого сервиса → реализована изоляция.

2. **Проверка работы контейнеров**

   * Flask-приложение доступно по назначенному порту.
   * Контейнеры корректно обрабатывают запросы внутри своей сети.

✅ **Вывод:** изоляция позволяет безопасно запускать несколько сервисов, не мешая друг другу, и предотвращает конфликт портов и данных.

---

## ⚠️ Плохие практики при работе с контейнерами

Даже используя хороший Dockerfile, можно накосячить при эксплуатации контейнеров:

1. **Запуск без ограничений ресурсов**

   * Команда: `docker run myapp`
   * Риск: контейнер может занять всю CPU и RAM на хосте → система тормозит.
   * Правильнее: `docker run -m 512m --cpus=1.0 myapp`

2. **Монтирование volumes с root-правами**

   * Команда: `docker run -v /host/data:/app/data myapp`
   * Риск: возможны проблемы с доступом к файлам или угрозы безопасности.
   * Правильнее: `docker run -v /host/data:/app/data:delegated --user 1001:1001 myapp`

---

- [Лабораторная 1](reports/lab1_report.md)
- [Лабораторная 2](reports/lab2_report.md)

---

## 🎯 Выводы

* Контейнеризация — мощный инструмент для изоляции приложений.
* Dockerfile и Docker Compose позволяют строить воспроизводимые и управляемые среды.
* Даже при корректной сборке образа важно правильно управлять **ресурсами и доступом** к хост-системе.
* Лабораторные закрепили навыки работы с контейнерами, настройкой сетей и безопасным запуском сервисов.

